import { EventEmitter } from 'events';
import { TrackingEvent, ConversionEvent, AttributionData } from '../types';

export class ConversionTracker extends EventEmitter {
  // ... implementation will be added in the next steps
} 